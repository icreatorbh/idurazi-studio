const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('fs'); const os=require('os'); const path=require('path'); const {ArchiveRepository}=require('../packages/archive-repository');
test('archive upsert search and link',()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'idurazi-')); const r=new ArchiveRepository(path.join(dir,'a.json')); const p=r.upsert('people',{nameAr:'محمد الشهاب',occupation:'معلم'}); const e=r.upsert('episodes',{title:'حلقة التعليم'}); r.link({fromType:'people',fromId:p.id,toType:'episodes',toId:e.id,relation:'guest_in'}); assert.equal(r.search('معلم').length,1); assert.equal(r.getLinked('people',p.id)[0].item.title,'حلقة التعليم');});
